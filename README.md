A CPU emulator and assembler college project.

Note some of the commits are duplicated becausee originally the repo was split a folder for each project revision instead of a branch.
and commits were applied to multiple directories at the same time.
